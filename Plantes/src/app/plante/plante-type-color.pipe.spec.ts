import { PlanteTypeColorPipe } from './plante-type-color.pipe';

describe('PlanteTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanteTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
